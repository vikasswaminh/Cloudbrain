{
  "name": "brain-cli-v2",
  "version": "1.0.0",
  "description": "CloudBrain CLI v2 — powered by Vercel AI SDK",
  "type": "commonjs",
  "bin": {
    "brain": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "npx esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=cjs",
    "build:exe": "npm run build && npx pkg dist/index.js --targets node18-win-x64 --output dist/brain.exe",
    "test": "tsx src/index.ts --version"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.24",
    "@ai-sdk/openai-compatible": "^2.0.30",
    "ai": "^4.3.19",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "inquirer": "^10.0.0",
    "ora": "^8.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
